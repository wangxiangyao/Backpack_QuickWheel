# 🧪 立即开始测试 - 配件掉落快速验证

## ✅ 代码已修改完成

`src/AttachmentSystem/AttachmentManager.cs:265-283` 已添加测试代码：
- ✅ 所有配件将同时获得 **Bullet** 标签（权重90%）
- ✅ 所有配件将同时获得 **Accessory** 标签（生产用）

## 🚀 测试步骤（5分钟完成）

### 步骤1：编译MOD
```bash
# 使用你的编译方式编译项目
# 确保编译成功无错误
```

### 步骤2：启动游戏并验证标签（30秒）

1. **启动游戏并加载MOD**
2. **进入游戏场景**
3. **按F8键**
4. **查看Unity控制台输出**

**预期看到的关键输出**：
```
⚠️ 测试模式：为物品 网兜 添加Bullet标签（权重90%，极易触发掉落）
⚠️ 测试模式：为物品 水壶袋 添加Bullet标签（权重90%，极易触发掉落）
...（其他所有配件）

=== 搜索结果 ===
找到 XX 个物品
  - 网兜 (TypeID: 349100, Quality: 2)
    标签: [SidePocket_Small, Bullet, Accessory]  ← 关键：必须同时有这三个标签！
  包含Accessory标签: ✅ 是
```

**验证点**：
- ✅ 每个配件都有警告信息 "测试模式：添加Bullet标签"
- ✅ 每个配件的标签列表包含 `Bullet` 和 `Accessory`
- ✅ "包含Accessory标签: ✅ 是"

**如果失败**：
- ❌ 没有看到 "测试模式" 警告 → 检查代码是否正确修改
- ❌ 标签中没有 Bullet → 检查 FindSystemTag("Bullet") 是否返回null
- ❌ 完全没有输出 → 检查F8热键是否正确绑定

### 步骤3：开箱测试（1-3分钟）

**找到测试箱子**：

从你的 `GameSource/LootBoxDebug.json` 中找到的Bullet箱子：

```
推荐测试位置：
LootBox_Bullet_15
  坐标: (324.89, 0.00, 192.50)
  配置: Bullet(90%), Explosive(5%), Accessory(3%)

预期结果：91.8% 概率掉落配件（Bullet权重90/98）
```

**其他Bullet箱子**：
- 搜索 `LootBox_Bullet_` 开头的箱子
- 共有9个包含Bullet标签的箱子

**测试过程**：
1. 找到箱子位置
2. 首次打开（重要：必须首次打开，已打开的箱子不会重新生成物品）
3. 查看掉落物品

**预期结果**：
- ✅ 开1-3个箱子就能获得配件
- ✅ 配件可以正常拾取
- ✅ 配件可以安装到背包插槽

**如果开5个箱子都没有配件**：
- 检查F8验证是否通过
- 检查箱子是否首次打开（已打开的箱子物品已固定）
- 查看Unity控制台是否有错误信息

### 步骤4：测试成功后立即清理

**⚠️ 重要：测试成功后必须立即恢复生产配置！**

打开 `src/AttachmentSystem/AttachmentManager.cs`，**注释掉或删除** 第265-283行：

```csharp
// ========================================
// 🧪 测试模式：添加Bullet标签（权重90%，极易触发）
// ⚠️ 警告：测试完成后必须注释掉以下代码块！
// ⚠️ 不要发布包含此代码的版本到生产环境！
// ========================================
/*  ← 添加注释开始
Tag bulletTag = FindSystemTag("Bullet");
if (bulletTag != null)
{
    if (!item.Tags.Contains(bulletTag))
    {
        item.Tags.Add(bulletTag);
        Debug.LogWarning($"⚠️ 测试模式：为物品 {item.DisplayName} 添加Bullet标签（权重90%，极易触发掉落）");
    }
}
else
{
    Debug.LogError("未找到Bullet标签！测试模式无法启用");
}
*/  ← 添加注释结束
// ======================================== 测试代码结束 ========================================
```

**或者直接删除整个测试代码块**（推荐）

**验证清理**：
1. 重新编译
2. 启动游戏按F8
3. 确认配件**只有** Accessory 标签，**没有** Bullet 标签
4. 确认没有 "测试模式" 警告信息

## 📋 测试检查清单

### 准备阶段
- [x] 代码已修改（Bullet标签已添加）
- [ ] 编译MOD成功
- [ ] 了解如何恢复生产配置

### F8验证阶段
- [ ] 启动游戏并加载MOD
- [ ] 按F8键
- [ ] 看到 "测试模式" 警告信息
- [ ] 配件同时有 Bullet 和 Accessory 标签
- [ ] "包含Accessory标签: ✅ 是"

### 开箱测试阶段
- [ ] 找到Bullet箱子位置
- [ ] 开第1个箱子 - 结果：______
- [ ] 开第2个箱子 - 结果：______
- [ ] 开第3个箱子 - 结果：______
- [ ] 配件可以正常拾取
- [ ] 配件可以安装到背包插槽

### 清理阶段
- [ ] 注释或删除测试代码（第265-283行）
- [ ] 重新编译验证
- [ ] F8确认只有Accessory标签
- [ ] 没有 "测试模式" 警告
- [ ] 可以提交代码

## 🎯 成功标准

**F8验证通过**：
```
标签: [SidePocket_Small, Bullet, Accessory]
包含Accessory标签: ✅ 是
```

**开箱测试通过**：
```
开1-3个Bullet箱子 → 获得配件
配件可用 → 可安装到背包
```

**清理验证通过**：
```
标签: [SidePocket_Small, Accessory]  ← 只有Accessory，没有Bullet
无 "测试模式" 警告信息
```

## ❌ 常见问题

### Q1: F8没有输出
**A**:
- 检查热键是否冲突
- 查看 `LootDebugInputPatch` 是否正确应用
- 尝试重新加载MOD

### Q2: 配件没有Bullet标签
**A**:
- 检查代码是否正确修改
- 查看控制台是否显示 "未找到Bullet标签"
- 确认 LoadSystemTags() 正常执行

### Q3: 开10个箱子都没有配件
**A**:
- F8验证是否通过
- 确认箱子是首次打开（不是已经打开过的）
- 查看 Unity 控制台错误信息
- 验证 ItemAssetsCollection.Search() 返回结果

### Q4: 测试成功，如何切换回生产模式
**A**:
- 注释或删除第265-283行测试代码
- 重新编译
- F8验证只有Accessory标签

## 📞 需要帮助？

如果遇到问题，请检查：
1. Unity控制台完整日志
2. F8输出的详细信息
3. 编译是否有警告或错误
4. MOD是否正确加载

## 更新历史

- 2025-11-01: 创建测试指南，代码已修改完成
